pipeline {
    agent any
    
    options {
        buildDiscarder(logRotator(numToKeepStr: '5'))
    }
    
    environment {
        SONAR_HOST_URL = http://98.70.57.4:9000
        SONAR_AUTH_TOKEN = sqp_a3fcc538f3211ef0c3a23858ac769325fcab4183
    }
    
    stages {
        stage('Scan') {
            steps {
                withSonarQubeEnv('Sonarqube') { 
                    sh './mvnw clean verify sonar:sonar'
                }
            }
        }
    }
}
